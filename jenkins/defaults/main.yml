---

 jenkins:
   plugins:
     - dashboard-view
     - build-name-setter
     - build-timeout
     - config-file-provider
     - credentials-binding
     - embeddable-build-status
     - rebuild
     - ssh-agent
     - throttle-concurrents
     - timestamper
     - ws-cleanup
     - warnings
     - junit
     - workflow-aggregator
     - github-organization-folder
     - jenkins-multijob-plugin
     - parameterized-trigger
     - copyartifact
     - git
     - git-parameter
     - Github
     - gitlab-plugin
     - repo
     - ssh-slaves
     - pam-auth
     - ldap
     - role-strategy
     - mailer
     - email-ext
     - emailext-template #adbf82ce82124fe
     - publish-over-ssh  #publish artifacts over ssh
     - ssh
     - Kubernetes
   python_libs:
     - boto3
   python3_libs:
     - boto3

 terraform:
   version: 0.12.24
   source_url: "https://releases.hashicorp.com/terraform"

 helm_utils:
   - snapd
   - python-pip

 helm_url:
   - https://get.helm.sh/helm-v3.2.0-linux-amd64.tar.gz
